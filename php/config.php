<?php
/**
 * Конфигурация обработчиков форм PEMO Pumps.
 * Один файл — один источник правды для e-mail получателя и общих настроек.
 * Меняете адрес получателя здесь, в коде обработчиков ничего трогать не нужно.
 */

return [
    // Куда уходят все заявки и отзывы с сайта
    'mail_to' => 'fitnesshousespb@gmail.com',

    // Адрес и имя отправителя. Рекомендуется указывать адрес в ВАШЕМ домене
    // (например no-reply@pemopumps.ru), а не адрес клиента и не Gmail —
    // иначе письма с высокой вероятностью попадут в спам у получателя.
    // Реальный e-mail клиента добавляется в текст письма и в Reply-To,
    // так что ответить "как есть" всё равно можно.
    'mail_from' => 'no-reply@pemopumps.ru',
    'mail_from_name' => 'PEMO Pumps — сайт',

    // Темы писем по типу формы
    'subject_lead' => 'Заявка с сайта PEMO Pumps',
    'subject_review' => 'Новый отзыв на сайте PEMO Pumps',

    // Разрешённые источники (Origin) для CORS.
    // Добавьте сюда домен, на котором реально будет жить сайт.
    'allowed_origins' => [
        'http://localhost:5173',
        'https://pemopumps.ru',
        'https://www.pemopumps.ru',
    ],

    // Антиспам: не более max_requests обращений с одного IP за окно времени
    'rate_limit' => [
        'max_requests' => 5,
        'window_seconds' => 600, // 10 минут
    ],

    // Локальный бэкап заявок в CSV на случай, если письмо не дойдёт
    // (частая ситуация с mail() на shared-хостинге). Настоятельно рекомендуется
    // держать включённым — это единственная страховка от потери лида.
    'log_to_csv' => true,
];
